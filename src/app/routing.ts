const BASE = {
  ROOT: '/table-view',
}

const TABLEVIEW = {
  ROOT: '/table-view',
}

const CHARTVIEW = {
  ROOT: '/chart-view',
}

export const routerConfig = {
  BASE: {
    routerPath: BASE.ROOT,
  },
  CHARTVIEW: {
    routerPath: CHARTVIEW.ROOT,
  },
  TABLEVIEW: {
    routerPath: TABLEVIEW.ROOT,
  },
}
