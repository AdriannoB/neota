const BASE = {
  ROOT: '/',
}

const DASHBOARD = {
  ROOT: '/dashboard',
}

export const routerConfig = {
  BASE: {
    routerPath: BASE.ROOT,
  },
  DASHBOARD: {
    routerPath: DASHBOARD.ROOT,
  },
}
